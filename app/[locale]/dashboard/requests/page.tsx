import { RoutePlaceholder } from "@/components/route-placeholder";

export default function RequestsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Dashboard"
      title="Pending requests"
      description="Placeholder for appointment requests awaiting supervisor review."
    />
  );
}
