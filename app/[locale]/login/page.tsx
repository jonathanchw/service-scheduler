import { RoutePlaceholder } from "@/components/route-placeholder";

export default function LoginPage() {
  return (
    <RoutePlaceholder
      eyebrow="Internal access"
      title="Login"
      description="Placeholder for internal user authentication."
    />
  );
}
